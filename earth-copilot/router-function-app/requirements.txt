# Azure Functions Deployment Requirements
# NOTE: This file is specifically for Azure Functions deployment.
# For local development, use the root requirements.txt instead.

# Azure Functions Core Requirements (Function-specific)
azure-functions==1.23.0
azure-functions-worker>=1.0.0,<2.0.0

# HTTP and JSON handling
requests==2.32.5
aiohttp==3.12.15
httpx>=0.27.0

# Logging and monitoring
# azure-monitor-opentelemetry>=1.0.0  # Commented out due to build issues

# Pattern matching
regex==2023.12.25

# TemporalResolver dependencies
python-dateutil==2.9.0.post0

# Semantic Kernel for intelligent query translation (compatible versions)
semantic-kernel==1.36.2
pydantic==2.11.9  # CRITICAL: Compatible version for semantic-kernel 1.36.2
pydantic-core==2.33.2  # Compatible core version for pydantic 2.11.9

# OpenAI integration
openai==1.107.2

# STAC/Earth observation (required for integrated STAC search functionality)
pystac-client>=0.7.0
planetary-computer>=1.0.0
pystac>=1.10.0

# Geospatial processing (required for STAC search)
shapely>=2.0.0
geojson>=3.0.0

# Core Python utilities
python-dotenv>=1.0.0
